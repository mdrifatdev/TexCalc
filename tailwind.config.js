/** @type {import('tailwindcss').Config} */
// Tailwind v4 — theme tokens are configured in src/index.css via @theme {}
// This file is kept for tooling compatibility (editor autocomplete, etc.)
export default {
  content: [
    './index.html',
    './src/**/*.{js,ts,jsx,tsx}',
  ],
}
